<div class="content_main">
  <main>
    <section class="hero-section">
      <div class="glass-container">
        <h1 class="fade-in">
          <span>Gana Premios Increíbles</span>
        </h1>
        <p class="slide-up">
          Participa en nuestras rifas online. ¡Boletos asequibles, premios extraordinarios!
        </p>
        <a href="/rifasact" class="hero-btn">Ver Rifas Activas</a>
      </div>
    </section>
  </main>
</div>

<style>
  /* Reset y base */
  body, html {
    margin: 0;
    padding: 0;
    height: 100%;
    font-family: 'Montserrat', sans-serif;
  }

  main {
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
    background: linear-gradient(135deg, #2a7a2a, #38a14b);
    overflow: hidden;
    padding: 1rem;
  }

  .hero-section {
    width: 100%;
    max-width: 900px;
    padding: 2rem;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  /* Caja vidrio (glassmorphism) */
  .glass-container {
    background: rgba(255, 255, 255, 0.15);
    border-radius: 25px;
    padding: 3rem 4rem;
    box-shadow:
      0 8px 32px 0 rgba(31, 38, 135, 0.37);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.18);
    text-align: center;
    color: #e6ffe6;
    max-width: 100%;
  }

  .glass-container h1 {
    font-size: 3.8rem;
    font-weight: 900;
    margin-bottom: 1rem;
    letter-spacing: 2px;
    text-shadow:
      0 0 10px rgba(72, 136, 72, 0.8),
      0 0 20px rgba(72, 136, 72, 0.6);
  }

  .glass-container h1 span {
    color: #a0f0a0;
    text-shadow:
      0 0 15px #a0f0a0,
      0 0 30px #a0f0a0;
  }

  .glass-container p {
    font-size: 1.5rem;
    margin-bottom: 2.5rem;
    font-weight: 500;
    text-shadow: 0 0 8px rgba(0, 0, 0, 0.3);
  }

  .hero-btn {
    background: linear-gradient(45deg, #38a14b, #2bb03a);
    color: white;
    padding: 1.2rem 3rem;
    font-size: 1.3rem;
    font-weight: 700;
    border-radius: 50px;
    border: none;
    cursor: pointer;
    box-shadow:
      0 0 10px #38a14b,
      0 0 20px #2bb03a;
    transition: all 0.3s ease;
    text-decoration: none;
    display: inline-block;
    position: relative;
    overflow: hidden;
  }

  .hero-btn::before {
    content: "";
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: linear-gradient(
      60deg,
      rgba(255, 255, 255, 0.3) 0%,
      rgba(255, 255, 255, 0.1) 50%,
      rgba(255, 255, 255, 0.3) 100%
    );
    transform: rotate(25deg);
    animation: shine 3s linear infinite;
    pointer-events: none;
  }

  .hero-btn:hover,
  .hero-btn:focus {
    background: linear-gradient(45deg, #2bb03a, #1f7a1f);
    box-shadow:
      0 0 15px #2bb03a,
      0 0 30px #1f7a1f;
    transform: scale(1.05);
    outline: none;
  }

  @keyframes shine {
    0% {
      transform: rotate(25deg) translateX(-100%);
    }
    100% {
      transform: rotate(25deg) translateX(100%);
    }
  }

  /* Animaciones de texto */
  .fade-in {
    opacity: 0;
    animation: fadeInUp 1.8s ease forwards;
  }

  .slide-up {
    opacity: 0;
    animation: slideUp 1.8s ease forwards;
    animation-delay: 0.4s;
  }

  @keyframes fadeInUp {
    0% {
      opacity: 0;
      transform: translateY(40px) scale(0.95);
    }
    100% {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  @keyframes slideUp {
    0% {
      opacity: 0;
      transform: translateY(30px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsividad */
  @media (max-width: 768px) {
    main {
      padding: 1rem;
    }

    .hero-section {
      padding: 1rem;
    }

    .glass-container {
      padding: 2rem 2.5rem;
    }

    .glass-container h1 {
      font-size: 2.8rem;
    }

    .glass-container p {
      font-size: 1.2rem;
      margin-bottom: 2rem;
    }

    .hero-btn {
      font-size: 1.1rem;
      padding: 1rem 2.5rem;
    }
  }
</style>
