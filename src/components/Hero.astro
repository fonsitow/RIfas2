<div class="hero-container">
  <div class="hero-text">
    <h1>Gana Premios Increíbles</h1>
    <p>Participa en nuestras rifas online. ¡Boletos asequibles, premios extraordinarios!</p>
    <a href="/rifasact" class="hero-btn">Ver Rifas Activas</a>
  </div>
</div>

<style>
  .hero-container {
    background-image: url('/images/hero-bg.jpg'); /* Asegúrate de tener una imagen de fondo */
    background-size: cover;
    background-position: center;
    height: 70vh; /* Ajusta la altura a tu gusto */
    display: flex;
    justify-content: center;
    align-items: center;
    color: white;
    text-align: center;
    position: relative;
  }

  .hero-container::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0, 0, 0, 0.5); /* Overlay oscuro para mejor lectura del texto */
  }

  .hero-text {
    position: relative;
    z-index: 10;
    max-width: 800px;
    padding: 1rem;
  }

  .hero-text h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
  }

  .hero-text p {
    font-size: 1.5rem;
    margin-bottom: 2rem;
    text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.7);
  }

  .hero-btn {
    background-color: #4299e1;
    color: white;
    padding: 1rem 2rem;
    text-decoration: none;
    border-radius: 5px;
    font-size: 1.1rem;
    font-weight: bold;
    transition: background-color 0.3s ease;
  }

  .hero-btn:hover {
    background-color: #2b6cb0;
  }

  @media (max-width: 768px) {
    .hero-text h1 {
      font-size: 2.5rem;
    }
    .hero-text p {
      font-size: 1.2rem;
    }
  }
</style>