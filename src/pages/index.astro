---
import Layout from '../layouts/Layout.astro';
import Hero from '../components/Hero.astro';
import Section2 from '../components/Section2.astro';
---

<Layout>

  <main class="main-content">
    
    <Hero />

    <Section2 />

    <section id="funciona" class="funciona1" >
      <h2 class="titulo" >¿Cómo funciona?</h2>
      <div class="how-it-works-grid">
        <div class="step-card">
          <div class="step-number">1</div>
          <h3>Elige tu Rifa</h3>
          <p>Explora nuestras rifas y encuentra el premio que más te guste.</p>
        </div>
        <div class="step-card" >
          <div class="step-number">2</div>
          <h3>Compra Boletos</h3>
          <p>Selecciona la cantidad de boletos que deseas. ¡Más boletos, más oportunidades!</p>
        </div>
        <div class="step-card">
          <div class="step-number">3</div>
          <h3>Espera el Sorteo</h3>
          <p>Una vez completados los boletos, realizaremos el sorteo en vivo.</p>
        </div>
        <div class="step-card">
          <div class="step-number">4</div>
          <h3>¡Gana!</h3>
          <p>Si tu boleto es el ganador, te contactaremos para entregarte tu premio.</p>
        </div>
      </div>
    </section>

  </main>
 
</Layout>



<style>
.main-content{
  padding-bottom: 2rem;
}

  .titulo {
    text-align: center;
    font-size: 32px;
    color: #000000;
  }

  .funciona1 {
    background: rgba(255, 255, 255, 0.452); /* fondo semi-transparente */
    padding: 32px;
    box-shadow: inset 0 4px 35px rgba(0, 0, 0, 0.619);
    font-family: 'Montserrat', sans-serif;
  }
  
  .section-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 3rem 1.5rem;
    text-align: center;
  }
  
  .bg-dark {
    background-color: #2d4839;
    color: #e2e8f0;
  }

  .text-light {
    color: #e1e1e1;
  }

  .section-title {
    font-size: 2.5rem;
    margin-bottom: 1rem;
    color: #ffffff;
  }
  
  .section-subtitle {
    font-size: 1.25rem;
    color: #000000;
    margin-bottom: 3rem;
  }
  
  .rifas-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
    justify-content: center;
  }

  .how-it-works-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    margin-top: 2rem;
  }

  .step-card {
    background-color: #ffffff44;
    backdrop-filter: blur(10px saturate(120%));
    padding: 2rem 1.5rem;
    border-radius: 10px;
    text-align: center;
    box-shadow: inset 0 4px 35px rgba(0, 0, 0, 0.619);
    transition: transform 0.3s ease;
  }

  .step-card:hover {
    transform: translateY(-10px);
  }

  .step-number {
    font-size: 2rem;
    font-weight: bold;
    color: #030303;
    margin-bottom: 1rem;
  }

  .step-card h3 {
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
  }

  .step-card p {
    color: #000000;
    font-size: 
  }
  
  
</style>

